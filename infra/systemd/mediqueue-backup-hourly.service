[Unit]
Description=MediQueue hourly PostgreSQL recovery point
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=mediqueue-deploy
WorkingDirectory=/srv/mediqueue
ExecStart=/usr/bin/bash /srv/mediqueue/scripts/backup.sh --environment production --env-file /etc/mediqueue/production.env --type incr --confirm BACKUP:production
Nice=10
IOSchedulingClass=best-effort
IOSchedulingPriority=7
